# Optional: run WITHOUT on-prem n8n (forks / laptop only).
# Our server does NOT use this file.
#
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.without-n8n.yml --profile container-app up -d --build

services:
  n8n:
    profiles: ["opt-in-n8n"]

  api:
    environment:
      N8N_WEBHOOKS_ENABLED: ${N8N_WEBHOOKS_ENABLED:-false}
      N8N_WEBHOOK_URL: ${N8N_WEBHOOK_URL:-}

  worker:
    environment:
      N8N_WEBHOOKS_ENABLED: ${N8N_WEBHOOKS_ENABLED:-false}
      N8N_WEBHOOK_URL: ${N8N_WEBHOOK_URL:-}
